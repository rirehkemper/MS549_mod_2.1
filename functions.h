#pragma once
// functions.h
//header guards to prevent the app from running indefenitely
#ifndef FUNCTIONS_H
#define FUNCTIONS_H

//These functions are defined in functions.cpp
void countdown(int* time);
void tabata(int* activeTime, int* restTime, int* rounds);

#endif // FUNCTIONS_H